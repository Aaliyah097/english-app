# Local-only directories
/ai
/.claude
docs

# Dependencies
node_modules

# Build output (Next.js)
.next
/out

# Test / build caches
coverage
*.tsbuildinfo
.eslintcache

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
/.idea
/.vscode
*.swp
*.swo
*~

# Vercel CLI state
.vercel

# All dotenv files are ignored — secrets must never be committed. The one
# exception is .env.example, which IS tracked as a template for new clones.
.env
.env.*
!.env.example
